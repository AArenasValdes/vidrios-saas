# Roadmap completo de implementación — Página de Venta + Refactor

## Resumen ejecutivo

Se implementó una **feature completa de "Página de Venta"** que permite a cada empresa configurar su mini landing pública para captar leads, y se realizó un **refactor de centralización** de código duplicado + suite de tests.

**Estado**: Plan de 14 pasos completado al 100%. Refactor DRY + tests en curso (falta fix de 1 error TS en test del repository).

---

## FASE 1: Plan original — Página de Venta (14 pasos, COMPLETO)

### Paso 1 — Migración SQL: columnas landing en `organization_profile`

| Archivo | Estado |
|---|---|
| `supabase/migrations/20260507120000_landing_settings.sql` | CREADO |

16 columnas nuevas: `public_name`, `public_subtitle`, `public_zone`, `public_business_type`, `secondary_color`, `hero_mode` (CHECK image/gradient), `hero_image_url`, `hero_title`, `hero_subtitle`, `show_gallery`, `show_schedule`, `show_rating`, `rating_label`, `jobs_count_label`, `form_title`, `form_subtitle`, `is_published`.

### Paso 2 — Migración SQL: tabla `public_landing_gallery`

| Archivo | Estado |
|---|---|
| `supabase/migrations/20260507120100_landing_gallery.sql` | CREADO |

Tabla con id IDENTITY, FK a organizations + organization_profile (ON DELETE CASCADE), índice `(organization_id, sort_order)`, RLS + 4 policies (SELECT/INSERT/UPDATE/DELETE para authenticated con subquery a users).

### Paso 3 — Tipos TypeScript

| Archivo | Cambio | Estado |
|---|---|---|
| `src/features/organization-profile/types/organization-profile.ts` | +16 campos landing + `HeroMode` type | MODIFICADO |
| `src/features/landing-gallery/types/landing-gallery.ts` | Nuevo: `LandingGalleryItem`, `CreateLandingGalleryItemInput`, `UpdateLandingGalleryItemInput`, `ReorderLandingGalleryItemInput` | CREADO |

### Paso 4 — Repository organization-profile

| Archivo | Cambio | Estado |
|---|---|---|
| `src/features/organization-profile/repositories/organization-profile.repository.ts` | +16 columnas en `OrganizationProfileRow`, mapeo en `mapOrganizationProfile`, upsert con campos landing, `uploadHeroImage()`, **eliminada `sanitizeFileName` local** (extraída a `@/utils/sanitize-file-name.ts`) | MODIFICADO |

### Paso 5 — Feature landing-gallery completa

| Archivo | Estado |
|---|---|
| `src/features/landing-gallery/repositories/landing-gallery.repository.ts` | CREADO — CRUD con browser client, `uploadGalleryImage()`, **eliminada `sanitizeFileName` local** (extraída a `@/utils/sanitize-file-name.ts`) |
| `src/features/landing-gallery/repositories/landing-gallery-server.repository.ts` | CREADO — `getPublicGalleryByOrganizationId()` con admin client + `import "server-only"` |
| `src/features/landing-gallery/services/landing-gallery.service.ts` | CREADO — Lógica negocio, max 8 items (`MAX_GALLERY_ITEMS`), delega a repository |
| `src/features/landing-gallery/hooks/useLandingGallery.ts` | CREADO — Hook React con `gallery`, `isLoading`, `isUploading`, `uploadAndAddImage`, `updateImage`, `deleteImage`, `reorderImages` |

### Paso 6 — Service organization-profile

| Archivo | Cambio | Estado |
|---|---|---|
| `src/features/organization-profile/services/organization-profile.service.ts` | +defaults (`DEFAULT_SECONDARY_COLOR`, `DEFAULT_HERO_TITLE`, `DEFAULT_FORM_TITLE`, `DEFAULT_FORM_SUBTITLE`), +validaciones (`normalizeHeroMode`, `sanitizeSecondaryColor`), `resolveOrganizationProfile` con fallbacks, `uploadHeroImage()` | MODIFICADO |

### Paso 7 — Hook organization-profile

| Archivo | Cambio | Estado |
|---|---|---|
| `src/features/organization-profile/hooks/useOrganizationProfile.ts` | +`isUploadingHero` state, `uploadHeroImage(file)` function | MODIFICADO |

### Paso 8 — Página interna editor

| Archivo | Estado |
|---|---|
| `app/(pwa-app)/configuracion/pagina-venta/page.tsx` | CREADO — 935 líneas, 6 tabs: Identidad, Estilo visual, Hero, Galería, Formulario, Publicación |
| `app/(pwa-app)/configuracion/pagina-venta/page.module.css` | CREADO — 722 líneas CSS Modules |

### Paso 9 — Navegación app-shell

| Archivo | Cambio | Estado |
|---|---|---|
| `src/components/layout/app-shell.tsx` | +Nav item "Página de venta" (`LuGlobe`, `/configuracion/pagina-venta`), +prefetch, +enlace en account menu | MODIFICADO |

### Paso 10 — Landing pública actualizada

| Archivo | Cambio | Estado |
|---|---|---|
| `app/(landing-web)/solicitud/[empresa]/page.tsx` | Consume campos landing cuando `isPublished=true`: hero image/gradient, secondaryColor, formTitle/formSubtitle, galería real, rating, schedule | MODIFICADO |
| `app/(landing-web)/solicitud/[empresa]/page.module.css` | +estilos hero background image, galería, rating badge | MODIFICADO (ya existían) |

### Paso 11 — Tipos y repository solicitudes

| Archivo | Cambio | Estado |
|---|---|---|
| `src/features/solicitudes/types/solicitud-contacto.ts` | +16 campos en `SolicitudEmpresaPublicaConfig` | MODIFICADO |
| `src/features/solicitudes/repositories/solicitudes-contacto.repository.ts` | +16 columnas en `SolicitudEmpresaPublicaConfigRow`, +`ORGANIZATION_PROFILE_PUBLIC_SELECT` ampliado, +mapeo completo con defaults, **BUG FIX: eliminado código duplicado** | MODIFICADO |
| `app/(landing-web)/solicitud/[empresa]/solicitud-empresa-form.tsx` | +props `formTitle?`, `formSubtitle?` | MODIFICADO |

### Paso 12 — Validaciones

Ya implementadas en `organization-profile.service.ts` (paso 6).

### Paso 13 — Documentación SQL

| Archivo | Cambio | Estado |
|---|---|---|
| `supabase/docs/current_schema.sql` | +tabla `public_landing_gallery`, +16 columnas landing en `organization_profile`, +comments de columnas | MODIFICADO |
| `supabase/docs/database_map.md` | Ya tenía tabla 22 galería (actualizado en sesión anterior) | MODIFICADO (antes) |
| `supabase/docs/rls_policies.md` | +policy #17 `public_landing_gallery`, contadores 22/17 | MODIFICADO |
| `supabase/docs/seed_order.md` | +entrada galería nivel 2, +entrada #23 en nivel 6, +grafo dependencia | MODIFICADO |

### Paso 14 — QA TypeScript

Bugs encontrados y corregidos:

| Bug | Archivo | Fix |
|---|---|---|
| `LuGlobe2` no existe en react-icons/lu | `app-shell.tsx`, `pagina-venta/page.tsx` | Reemplazado por `LuGlobe` |
| `EMPTY_FORM` sin campos landing | `empresa/page.tsx` | Agregados 16 campos |
| `setForm` sin campos landing | `empresa/page.tsx` | Agregados 16 campos en efecto |
| Objeto manual `OrganizationProfile` sin campos landing | `public-cotizacion-approval.service.ts` | Agregados 16 campos |
| Mock profile sin campos landing | `useOrganizationProfile.test.tsx` | Agregados 16 campos + import corregido |
| Input/expect sin campos landing | `organization-profile.service.test.ts` | Agregados 16 campos en 2 tests |
| Mock admin client sin tipo | `public-cotizacion-approval.repository.test.ts` | Agregado `as never` |
| **Código duplicado en `mapSolicitudEmpresaPublicaConfig`** | `solicitudes-contacto.repository.ts` | Eliminado bloque duplicado (líneas 264-295) |

**Resultado**: `npx tsc --noEmit` pasa limpio.

---

## FASE 2: Refactor DRY + Tests (EN CURSO)

### Refactor sanitizeFileName — COMPLETO

| Archivo | Cambio | Estado |
|---|---|---|
| `src/utils/sanitize-file-name.ts` | CREADO — función `sanitizeFileName` centralizada | NUEVO |
| `src/features/organization-profile/repositories/organization-profile.repository.ts` | Eliminada función local, agregado `import { sanitizeFileName } from "@/utils/sanitize-file-name"` | MODIFICADO |
| `src/features/landing-gallery/repositories/landing-gallery.repository.ts` | Eliminada función local, agregado `import { sanitizeFileName } from "@/utils/sanitize-file-name"` | MODIFICADO |

**Nota**: `src/utils/cotizacion-pdf.ts` tiene `sanitizeFileNamePart` — variante diferente (sin puntos en regex, con `.slice(0, 50)`, sin colapsar guiones). No se unificó porque es semántica distinta (nombre de archivo PDF vs nombre de storage).

### Tests creados — 4 suites

| Archivo | Capa | Casos | Estado |
|---|---|---|---|
| `src/utils/__tests__/sanitize-file-name.test.ts` | Utilidad | 8 tests: minúsculas, diacríticos, caracteres inválidos, guiones consecutivos, puntos/guionesbajos, trim, vacío, espacios+mayúsculas | CREADO |
| `src/features/landing-gallery/services/__tests__/landing-gallery.service.test.ts` | Service | 7 tests: delegación get, agregar con espacio, sortOrder automático, rechazar 8 items, permitir 7, update, delete, reorder, upload | CREADO |
| `src/features/landing-gallery/repositories/__tests__/landing-gallery.repository.test.ts` | Repository | 7 tests: listar ordenado, error Supabase, label null, crear item, actualizar campos, eliminar, rechazar no-imagen, rechazar >10MB | CREADO |
| `src/features/landing-gallery/hooks/__tests__/useLandingGallery.test.tsx` | Hook | 9 tests: carga al montar, org null, uploadAndAdd, error sin org, updateImage, deleteImage, reorderImages, error en carga, error en upload, isUploading | CREADO |

### Pendiente — 1 error TS en test del repository

El test `landing-gallery.repository.test.ts:103` tiene `client.from` accesado sobre `as never`, que TS no permite. Fix: cambiar assertion de tipo o reestructurar el mock.

---

## Lista completa de archivos para revisión rápida

### Creados (9 + 4 tests = 13)

```
supabase/migrations/20260507120000_landing_settings.sql
supabase/migrations/20260507120100_landing_gallery.sql
src/features/landing-gallery/types/landing-gallery.ts
src/features/landing-gallery/repositories/landing-gallery.repository.ts
src/features/landing-gallery/repositories/landing-gallery-server.repository.ts
src/features/landing-gallery/services/landing-gallery.service.ts
src/features/landing-gallery/hooks/useLandingGallery.ts
app/(pwa-app)/configuracion/pagina-venta/page.tsx
app/(pwa-app)/configuracion/pagina-venta/page.module.css
src/utils/sanitize-file-name.ts                                          ← REFACTORIZADO
src/utils/__tests__/sanitize-file-name.test.ts                          ← TEST NUEVO
src/features/landing-gallery/services/__tests__/landing-gallery.service.test.ts   ← TEST NUEVO
src/features/landing-gallery/repositories/__tests__/landing-gallery.repository.test.ts ← TEST NUEVO (1 error TS)
src/features/landing-gallery/hooks/__tests__/useLandingGallery.test.tsx           ← TEST NUEVO
```

### Modificados (14 + 2 tests = 16)

```
src/features/organization-profile/types/organization-profile.ts
src/features/organization-profile/repositories/organization-profile.repository.ts  ← +refactor sanitizeFileName
src/features/organization-profile/services/organization-profile.service.ts
src/features/organization-profile/hooks/useOrganizationProfile.ts
src/features/solicitudes/types/solicitud-contacto.ts
src/features/solicitudes/repositories/solicitudes-contacto.repository.ts  ← +bug fix código duplicado
app/(landing-web)/solicitud/[empresa]/page.tsx
app/(landing-web)/solicitud/[empresa]/solicitud-empresa-form.tsx
src/components/layout/app-shell.tsx
app/(pwa-app)/configuracion/empresa/page.tsx
src/features/cotizaciones/public-approval/services/public-cotizacion-approval.service.ts
supabase/docs/current_schema.sql
supabase/docs/rls_policies.md
supabase/docs/seed_order.md
src/hooks/__tests__/useOrganizationProfile.test.tsx                      ← TEST CORREGIDO
src/services/__tests__/organization-profile.service.test.ts              ← TEST CORREGIDO
src/features/cotizaciones/public-approval/repositories/__tests__/public-cotizacion-approval.repository.test.ts ← TEST CORREGIDO
src/features/landing-gallery/repositories/landing-gallery.repository.ts  ← +refactor sanitizeFileName
```

---

## Bugs corregidos durante la implementación

1. **CRÍTICO** — `mapSolicitudEmpresaPublicaConfig` tenía código duplicado: después del return con campos landing existía un segundo bloque return legacy que nunca se ejecutaba pero creaba confusión. Eliminado.
2. `LuGlobe2` no existe en `react-icons/lu` → `LuGlobe`
3. `empresa/page.tsx` — EMPTY_FORM y setForm sin 16 campos landing
4. `public-cotizacion-approval.service.ts` — objeto manual OrganizationProfile incompleto
5. 3 archivos de test con mocks desactualizados tras agregar campos landing

---

## Pendiente para cerrar esta iteración

1. Fix TS error en `landing-gallery.repository.test.ts:103` — `client.from` sobre `as never`
2. Ejecutar `npx jest` para verificar que los 4 nuevos test suites pasan
3. Ejecutar `npx tsc --noEmit` para confirmar 0 errores
